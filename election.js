/*
- gonna need a for loop
- gonna need counters for each new letters and a way to identify a new letter
- a way to check whats already been counted in (indexOf())
- a way to classify each letter
- program must know alphabet in all caps and lower case
*/

function getWinner(groot){
    let capsAlpha = "ABCDEFGHIJKLMOPQRSTUVWXYZ"
    let lowerApla = "abcdefghijklmnopqrstuvwxyz"
    let emptyArryay = []

    for(let i = 0; i < groot.length; i++){
        
    }
}

console.log(getWinner(["A", "A", "A", "B", "B"]));
console.log(getWinner(["A", "A", "B", "B"]));
console.log(getWinner(["A", "B", "C", "D"]));
console.log(getWinner(["A", "A", "A", "B", "B", "C"]));

